.xlil version 0
.xlil module App
.func Identity : (i64) -> i64
.param %r0:i64
bb0.entry:
  ret %r0
.end
.func Answer : () -> i64
bb0.entry:
  %r0:i64 = const 42
  ret %r0
.end
.func main : () -> i32
bb0.entry:
  %r0:i32 = const.i32 0
  ret %r0
.end
.func Choose : () -> void
bb0.entry:
  %r0:bool = const.bool true
  br_if %r0, bb1, bb2
bb1.then:
  ret
bb2.else:
  ret
.end
